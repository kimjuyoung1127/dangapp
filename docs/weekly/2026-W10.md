# Week 2026-W10 Summary
> Generated: 2026-03-12 (nightly organizer)
> Covers: 2026-03-02 ~ 2026-03-04

## Parity IDs Touched
- DANG-INFRA-001
- DANG-ONB-001
- DANG-AUTH-001
- DANG-AUTH-002
- DANG-MAT-001
- DANG-CHT-001
- DANG-DLG-001
- DANG-WLK-001
- DANG-PRF-001
- DANG-B2B-001
- DANG-DES-001

## Route Changes

| Route | Status | Parity ID |
|-------|--------|-----------|
| /chat | Ready → QA | DANG-CHT-001 |
| /chat/[id] | Ready → QA | DANG-CHT-001 |
| /home | InProgress → QA | DANG-MAT-001, DANG-DES-001 |
| /onboarding | InProgress → QA | DANG-INFRA-001, DANG-ONB-001 |
| /danglog | Complete | DANG-DLG-001 |
| /danglog/[id] | Complete | DANG-DLG-001 |
| /profile | Complete | DANG-PRF-001 |
| /schedules | Complete | DANG-WLK-001 |
| /login | Refactored | DANG-AUTH-001 |

## Key Work Summary

### Schema & Infrastructure (DANG-INFRA-001)
- 6 migrations applied: initial_schema → wave1_storage_policies (29 tables, 4 storage buckets, 65 RLS policies).
- High-fidelity schema phase 1–3: auth sync trigger, `match_guardians_v2` RPC with weighted scoring, `dog_ownership` for family accounts, `partner_places`/`reservations` for B2B, `reports` for user moderation.
- Emergency patches: `set_guardian_location` security definer check, `mappers.ts` PostGIS coordinate formatting, `activity_times` normalization.
- Remote Supabase runtime verified: both RPCs deployed and smoke-tested.

### Onboarding (DANG-ONB-001)
- Progressive 8-step flow with 3 required fields (nickname, dog_name, address_name), rest optional.
- RHF + Zod validation, ToggleChip CVA, PageSlide MotionWrapper.
- Step 5 real file upload (Supabase Storage), Step 6 phone auth UI (3-min timer + skip debug), Step 7 full submit (guardian + dog + consent_logs INSERT).
- Existing guardian redirect logic added.

### Auth Integration (DANG-AUTH-001, DANG-AUTH-002)
- OAuth callback (`/auth/callback/route.ts`) with `@supabase/ssr` cookie-baking pattern.
- Middleware hardened: path-group state machine, `getUser()` (not `getSession()`), `maybeSingle()` for safe new-user handling.
- Google OAuth login, middleware gating (onboarding redirect for new users) validated end-to-end.
- **⚠️ Risk flagged:** `DebugNavigator.tsx` has `isDev = true` hardcoded — must be changed to `process.env.NODE_ENV === 'development'` before production.

### Matching & Home (DANG-MAT-001)
- `useCurrentGuardian` hook (auth → guardians + dogs).
- `useMatchingGuardians` with client-side `usage_purpose` filter, `useCreateMatchAction` with mutual match detection.
- `MatchCard` fully unbound from dummy data; `MutualMatchModal`, `MatchEmptyState`, `IncompleteProfileBanner` added.
- `useGetOrCreateChatRoom` connected for match → chat transition.

### Chat (DANG-CHT-001)
- `useChatRooms` (realtime INSERT detection, unread count badge), `useChatRoom`, `useSendMessage`, `useMarkAsRead`, `useChatPartner`, `useGetOrCreateChatRoom`.
- Both `/chat` and `/chat/[id]` fully unbound from hardcoded IDs.
- **⚠️ Risk:** N+1 query per room for partner/last-message/unread — optimize with RPC later.

### DangLog (DANG-DLG-001)
- `useDangLogCounts`, `useDangLogCollaborators`, `useInviteCollaborator`, `useAcceptInvite` added to `useDangLog.ts`.
- All mock data removed; real data bound via `useCurrentGuardian()`.
- `ShareModal`, `DangLogEmptyState` components added; collaborator avatars on detail page.
- **⚠️ Risk:** N+1 per feed item; `/danglog/invite/[token]` route not yet implemented.

### Profile (DANG-PRF-001)
- `useUpdateGuardian`, `useUpdateDog`, `useNotificationSettings`, `useUpdateNotificationSettings`, `useProfileStats` added.
- `EditProfileSheet` (2-tab BottomSheet, RHF+Zod), `NotificationSettings` (5-toggle UPSERT).
- Stats from 3 queries (reviews + schedules organizer + participant) — future RPC candidate.

### Schedules / Walk (DANG-WLK-001)
- `useSchedule`, `useWalkRecord`, `useWalkReview` hooks created.
- `WalkRecordForm` BottomSheet (RHF+Zod, image upload to `walk-records` bucket).
- Completed tab: "산책 기록" + "후기 작성" dual actions.

### Planning (3-02)
- Parity matrix, route board, skill matrix, master execution plan initialized.
- Wave 1 schema/storage migration files authored.
- Frontend type-check + ESLint passed.

## Daily Logs Included

- 2026-03-02 (03-02): page-chat.md, page-home.md, page-onboarding.md
- 2026-03-02 (3-02 — malformed folder): page-planning.md
- 2026-03-03 (03-03): page-danglog.md, page-onboarding-survey.md, page-profile.md, page-schedules.md, review-auth-e2e.md, review-authentication-onboarding.md, review-onboarding-survey.md
- 2026-03-04 (03-04): schema-enhancement.md
