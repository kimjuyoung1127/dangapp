# modes components

UI components for mode selection and unlock behavior.

- `ModeCard`: show mode state, benefits, and unlock status.
- `ModeUnlockDialog`: explain unlock conditions and next actions.
- State should flow through `useModeStore` and `useMode.ts`.
