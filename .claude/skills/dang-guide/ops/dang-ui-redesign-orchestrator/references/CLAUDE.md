<!-- File: Local guide for UI redesign orchestration reference documents. -->
# references

## Purpose
- Hold reusable reference summaries and templates for the UI redesign orchestration skill.

## Structure
- `ui-reference-clusters.md`: distilled reference territories and product-fit notes.
- `screen-mapping-template.md`: reusable route-to-reference mapping template.

## Responsibilities
1. Keep route mapping inputs concise and reusable.
2. Store normalized research outcomes instead of raw report dumps.
3. Avoid duplicating procedural rules from `SKILL.md`.
